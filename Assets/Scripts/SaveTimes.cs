using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveTimes : MonoBehaviour
{

    // private static void SaveTime(SaveTimes data, string subPath) 
    // {
    //     var binaryFormatter = new BinaryFormatter();
    //     var fullPath = Path.Combine(Application.persistentDataPath, subPath);
    //     using(FileStream fileStream = File.Open(fullPath, FileMode.OpenOrCreate)) {
    //         binaryFormatter.Serialize(fileStream, data);
    //     }
    // }



}
